---
import StandardContentPage from "@/components/StandardContentPage.astro";
import data from "@/data/contact.json";

const contactData = data as any;
const header = contactData.header || { tag: "Comunícate", heading: "Contacto", subtitle: "" };
const components: any[] = Array.isArray(contactData.components) ? contactData.components : [];
---

<StandardContentPage
  title={header.pageTitle || header.heading}
  description={header.pageDescription || header.subtitle}
  {header}
  {components}
/>
