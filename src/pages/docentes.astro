---
import StandardContentPage from "@/components/StandardContentPage.astro";
import data from "@/data/docentes.json";

const header = data.header;
const components: any[] = Array.isArray((data as any).components) ? (data as any).components : [];
---

<StandardContentPage
  title={(data as any).pageTitle || header.heading}
  description={(data as any).pageDescription || header.subtitle}
  {header}
  {components}
/>
