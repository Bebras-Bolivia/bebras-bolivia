---
import TeacherInstructions from "@/components/TeacherInstructions";

interface Item {
  title: string;
  desc: string;
}

interface Tab {
  id: string;
  label: string;
  heading: string;
  items: Item[];
  color?: string;
}

interface Props {
  sectionTag: string;
  heading: string;
  subtitle: string;
  tabs: Tab[];
  accent?: string;
  cardPalette?: string[];
}

const { sectionTag, heading, subtitle, tabs = [], accent, cardPalette } = Astro.props;
---

<div class="reveal-section w-full mx-auto max-w-6xl mb-8 lg:mb-10 px-4 sm:px-6">
  <TeacherInstructions sectionTag={sectionTag} heading={heading} subtitle={subtitle} tabs={tabs} accent={accent} cardPalette={cardPalette} client:visible />
</div>
